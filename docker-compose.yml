version: "3.9"

services:
  referee:
    build:
      context: .
      dockerfile: Dockerfile
      target: referee
    volumes:
      - ./:/app
    working_dir: /app
    command: ["bash", "scripts/run_referee.sh"]
